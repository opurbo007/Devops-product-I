{
  "name": "shared-prisma",
  "version": "1.0.0",
  "description": "Shared Prisma ORM package — single source of truth for all Postgres models (users, orders, stock, payments, shipments, carts, notifications + outbox/processed_events).",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "generate": "node ./scripts/prisma-with-url.mjs generate",
    "migrate:dev": "node ./scripts/prisma-with-url.mjs migrate dev",
    "migrate:deploy": "node ./scripts/prisma-with-url.mjs migrate deploy",
    "db:push": "node ./scripts/prisma-with-url.mjs db push",
    "studio": "node ./scripts/prisma-with-url.mjs studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "prisma": "^6.0.0",
    "typescript": "^7.0.2"
  }
}
