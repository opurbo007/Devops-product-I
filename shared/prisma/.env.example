# Per-service Postgres connection. Point at that service's own database —
# the shared schema is migrated into every DB (services only use their tables).
DATABASE_URL="postgresql://postgres:00000000@localhost:5432/app"
