# Dev-only placeholder secrets. Production: replace with SealedSecrets or
# external-secrets (Vault / cloud secret manager) — never commit real keys.
apiVersion: v1
kind: Secret
metadata:
  name: ecom-secrets
  namespace: ecom
type: Opaque
stringData:
  postgres-password: "00000000"
  jwt-access-secret: "dev-access-secret-change-me"
  jwt-refresh-secret: "dev-refresh-secret-change-me"
  jwt-internal-secret: "dev-internal-secret-change-me"
  stripe-secret-key: ""
